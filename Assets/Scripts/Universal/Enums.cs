using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Language{
    English = 0,
    Chinese = 1,
    Japanese = 2,
}

public enum Price{
    Default = 0,
    Custom = 1,
}