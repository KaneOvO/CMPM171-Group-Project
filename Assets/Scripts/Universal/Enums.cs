using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Language
{
    English = 0,
    Chinese = 1,
    Japanese = 2,
}

public enum Price
{
    Default = 0,
    Custom = 1,
}

public enum SceneType
{
    Opening = 0,
    Title = 1,
    Setting = 2,
    Credit = 3,
    InGame = 4,
    Ending = 5,
    Test = 6,
}